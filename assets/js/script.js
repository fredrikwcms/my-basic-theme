(function($) {

    $(document).ready(function() {
        // This will be run when DOM has signlaed that it has finished rendering all elements
        $('.card').on('click', function(e) {
        });
    });
})(jQuery);